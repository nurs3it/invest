# Investment App

Современное React + TypeScript приложение с Feature-Sliced Design архитектурой и собственной дизайн-системой.

## 🎨 Дизайн-система

Проект включает 4 темы оформления:
- **Светлая** - классическая светлая тема
- **Тёмная** - для работы в условиях низкой освещённости
- **Coffee** - тёплая мягкая тема
- **Доступная** - высококонтрастная тема для слабовидящих (WCAG AAA)

Шрифт: **Roboto** (Google Fonts)

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск dev сервера
```bash
npm run dev
```

Приложение откроется на http://localhost:3000

### Просмотр дизайн-системы
Откройте http://localhost:3000/ui для просмотра всех компонентов и тем.

## 📦 Технологии

- **React 18** - библиотека для создания интерфейсов
- **TypeScript** - строгая типизация
- **Vite** - быстрая сборка (SWC)
- **Tailwind CSS 3** - utility-first CSS
- **shadcn/ui** - библиотека компонентов
- **TanStack Query** - управление серверным состоянием
- **Redux Toolkit** - управление клиентским состоянием
- **React Router** - маршрутизация
- **Feature-Sliced Design** - архитектура проекта

## 📁 Структура проекта (FSD)

```
src/
├── app/           # Инициализация приложения
│   ├── providers/ # Провайдеры (Router, Store, Query, Theme)
│   ├── router/    # Маршрутизация
│   └── store/     # Redux store
├── pages/         # Страницы приложения
│   ├── home/      # Главная страница
│   ├── dashboard/ # Дашборд
│   ├── login/     # Страница входа
│   └── ui/        # Дизайн-система
├── features/      # Бизнес-фичи
│   └── auth/      # Аутентификация
├── entities/      # Бизнес-сущности
│   └── user/      # Пользователь
└── shared/        # Общий код
    ├── ui/        # UI компоненты
    ├── api/       # API клиент
    ├── lib/       # Утилиты
    └── config/    # Конфигурация
```

## 🎯 Доступные команды

```bash
npm run dev         # Запуск dev сервера
npm run build       # Production сборка
npm run preview     # Просмотр production сборки
npm run lint        # Проверка кода (ESLint)
npm run lint:fix    # Автоисправление ошибок
npm run format      # Форматирование кода (Prettier)
npm run type-check  # Проверка типов TypeScript
```

## 🎨 Работа с темами

### Использование ThemeSwitcher
```tsx
import { ThemeSwitcher } from '@shared/ui/theme-switcher';

function MyComponent() {
  return <ThemeSwitcher />;
}
```

### Программное переключение темы
```tsx
import { useTheme } from '@app/providers/with-theme';

function MyComponent() {
  const { theme, setTheme } = useTheme();
  
  return (
    <button onClick={() => setTheme('dark')}>
      Переключить тему
    </button>
  );
}
```

Доступные темы: `'light' | 'dark' | 'coffee' | 'accessible'`

## 🧩 Компоненты

### Обычные (shadcn)
```tsx
<Button variant="default">Кнопка</Button>
<Card>
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
  </CardHeader>
</Card>
```

### Примеры использования
```tsx
<Button variant="default">Кнопка</Button>
<Card>
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
  </CardHeader>
  <CardContent>Содержимое</CardContent>
</Card>
```

## 🛣️ Маршруты

- `/` - Главная страница
- `/dashboard` - Дашборд
- `/login` - Страница входа
- `/ui` - Дизайн-система

## 📝 Добавление новой страницы

1. Создайте файл в `src/pages/[name]/ui/[name]-page.tsx`
2. Экспортируйте через `src/pages/[name]/index.ts`
3. Добавьте маршрут в `src/app/router/index.tsx`

## 🎨 Path aliases

Проект использует алиасы для удобного импорта:

```tsx
import { Button } from '@shared/ui';
import { LoginForm } from '@features/auth';
import { useAppSelector } from '@app/store/hooks';
```

- `@app/*` - application layer
- `@pages/*` - pages
- `@features/*` - features
- `@entities/*` - entities
- `@shared/*` - shared code

## 🌐 API

API клиент настроен в `src/shared/api/client.ts`:
- Автоматическая вставка токена
- Обработка ошибок 401
- Базовый URL из переменных окружения

### Использование с TanStack Query
```tsx
import { useQuery } from '@tanstack/react-query';
import { apiClient } from '@shared/api';

export const useData = () => {
  return useQuery({
    queryKey: ['data'],
    queryFn: async () => {
      const { data } = await apiClient.get('/endpoint');
      return data;
    },
  });
};
```

## ⚙️ Переменные окружения

Создайте `.env` файл:
```env
VITE_API_URL=http://localhost:8000/api
```

## 📚 Документация

- Страница `/ui` - интерактивная демонстрация компонентов
- [shadcn/ui Documentation](https://ui.shadcn.com) - официальная документация компонентов

## 🔧 Настройка

### Tailwind CSS
Конфигурация в `src/index.css` - темы и цвета определены через CSS-переменные в `@theme`.

### TypeScript
Строгая типизация включена в `tsconfig.json`.

### ESLint + Prettier
Настроены для поддержания качества кода.

## 📄 Лицензия

MIT

